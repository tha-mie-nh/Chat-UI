# Required — must set these two values
GEMINI_API_KEY=your_gemini_api_key
AGENT_URL=http://your-backend-agent-url

# Optional — override if your backend is on a different host/port
# Default: http://backend:3001 (works within docker-compose network automatically)
# BACKEND_URL=http://your-be-host:3001
